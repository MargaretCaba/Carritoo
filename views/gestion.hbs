<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestión de Productos</title>
    <!-- Importar estilos de Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <!-- Barra de navegación -->
    <nav class="navbar navbar-expand-sm bg-body-tertiary">
        <div class="container-fluid">
            <a class="navbar-brand" href="">Sonidos del Cielo <i class="fas fa-guitar" style="color: #8C503A;"></i></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse justify-content-end navbar-collapse" id="navbarNavDropdown">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="carrito">Carrito</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="login">Log In</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/compras-realizadas">Gestion Compras</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Título de la página -->
    <h1>Gestión de Productos</h1>

<!-- Formulario para agregar un nuevo producto -->
<form action="/users/agregar-producto" method="POST">
    <!-- Campos del formulario para agregar un nuevo producto -->
    <button type="submit" class="btn btn-success">Agregar Producto</button>
</form>

<!-- Formulario para eliminar un producto existente -->
<form action="/users/eliminar-producto" method="POST">
    <!-- Campos del formulario para eliminar un producto -->
    <button type="submit" class="btn btn-danger">Eliminar Producto</button>
</form>

<!-- Formulario para editar un producto existente -->
<form action="/users/editar-producto" method="POST">
    <!-- Campos del formulario para editar un producto -->
    <button type="submit" class="btn btn-primary">Editar Producto</button>
</form>

    <table class="table">
        <h2>Almacen</h2>
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Precio</th>
                <th>Cantidad</th>
            </tr>
        </thead>
        <tbody>
            {{#each productos}}
                <tr>
                    <td>{{this.nombre}}</td>
                    <td>{{this.precio}}{{this.moneda}}</td>
                    <td>{{this.cantidad}}</td>
                </tr>
            {{/each}}
        </tbody>
    </table>

    <!-- Importar scripts de Bootstrap al final del body -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

